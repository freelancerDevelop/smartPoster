create database android;
use android;
create table admin (id int primary key auto_increment, username varchar(20), password varchar(20), url varchar(100));
insert into admin values (0,"admin","admin","localhost");
